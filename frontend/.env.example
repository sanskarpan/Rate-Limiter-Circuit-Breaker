# Base URL of the Go demo server that this playground talks to.
# Defaults to http://localhost:8080 (the address the demo server binds to).
# Copy this file to `.env.local` and adjust if your server runs elsewhere.
NEXT_PUBLIC_API_URL=http://localhost:8080
