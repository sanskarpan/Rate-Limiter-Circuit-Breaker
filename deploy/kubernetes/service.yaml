apiVersion: v1
kind: Service
metadata:
  name: resilience-demo
  namespace: default
  labels:
    app: resilience-demo
spec:
  selector:
    app: resilience-demo
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  type: ClusterIP
